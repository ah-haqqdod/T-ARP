# Source Code

`t_arp` directory contains the numeric implementation of the T-ARP algorithms.

`test` directory contains initial prototypes used to test the implementations. Notice that the code there might not be up-to-date with the latest version of the algorithms.

`benchmark` directory contains benchmarks used to evaluate the performance of the algorithms.
